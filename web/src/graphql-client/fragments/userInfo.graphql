fragment userInfo on User {
    username
    id
    fullName
    email
    address
    avatarUrl
    wallet {
      id
      availableBalance
      balance
    }
    identification {
      serial
      issuedBy
      issueDate
    }
}